---
title: Overview
description: Modular toolkit for building real-time analytical backends
---

import { Callout, CTACards, CTACard, BulletPointsCard, IconBadge, PathConfig, StaggeredCards, StaggeredCard, StaggeredContent, StaggeredCode, TypeScript, Python } from "@/components";
import { Database, Laptop, Rocket, Package, FileText, Info, Github } from "lucide-react";
import { Tabs } from "nextra/components";

# MooseStack

<div className="flex items-center gap-4 my-8 flex-wrap">
  <IconBadge Icon={PathConfig.quickstart.icon} label="5 minute setup" variant="moose" />
  <IconBadge Icon={Database} label="Pre-integrated ClickHouse, Redpanda, Temporal" variant="moose" />
  <IconBadge Icon={Laptop} label="Hot-reload development" variant="moose" />
</div>

Type-safe, code-first tooling for building real-time analytical backends--OLAP Databases, Data Streaming, ETL Workflows, Query APIs, and more.

## Get Started
```bash filename="Install Moose" copy
bash -i <(curl -fsSL https://fiveonefour.com/install.sh) moose
```

<CTACards rows={3} columns={1}>
  <CTACard
    title="5-minute Quickstart"
    description="Build your first analytics backend from scratch"
    ctaLink={PathConfig.quickstart.path}
    ctaLabel="Start Tutorial →"
    Icon={PathConfig.quickstart.icon}
    variant="default"
    orientation="horizontal"
  />
  <CTACard
    title="Add to existing ClickHouse"
    description="Works with your current data - zero migration required"
    ctaLink={PathConfig.fromClickhouse.path}
    ctaLabel="Add Moose Layer"
    Icon={PathConfig.fromClickhouse.icon}
    variant="default"
    orientation="horizontal"
  />
  <CTACard
    title="Bootstrap with Area Code"
    description="Start with a template for integrating Moose with complex microservices architectures"
    ctaLink="https://github.com/514-labs/area-code"
    ctaLabel="Star on GitHub"
    Icon={Github}
    variant="default"
    orientation="horizontal"
  />
</CTACards>

<br />

## Everything as Code

Declare all infrastructure (e.g. ClickHouse tables, Redpanda streams, APIs, etc.) and pipelines in pure TypeScript or Python. Your code auto-wires everything together, so no integration boilerplate needed.

<Tabs items={["TypeScript", "Python"]}>
<Tabs.Tab>
```ts filename="Complete Analytical Backend in 1 TS file" copy
import { Key, OlapTable, Stream, IngestApi, ConsumptionApi } from "@514labs/moose-lib";
 
interface DataModel {
  primaryKey: Key<string>;
  name: string;
}
// Create a ClickHouse table
export const clickhouseTable = new OlapTable<DataModel>("TableName");
 
// Create a Redpanda streaming topic
export const redpandaTopic = new Stream<DataModel>("TopicName", {
  destination: clickhouseTable,
});
 
// Create an ingest API endpoint
export const ingestApi = new IngestApi<DataModel>("post-api-route", {
  destination: redpandaTopic,
});
 
// Create consumption API endpoint
interface QueryParams {
  limit?: number;
}
export const consumptionApi = new ConsumptionApi<QueryParams, DataModel[]>("get-api-route", 
  async ({limit = 10}: QueryParams, {client, sql}) => {
    const result = await client.query.execute(sql`SELECT * FROM ${clickhouseTable} LIMIT ${limit}`);
    return await result.json();
  }
);
```
</Tabs.Tab>
<Tabs.Tab>
```python filename="Complete Analytical Backend in 1 Python file" copy
from moose_lib import Key, OlapTable, Stream, StreamConfig, IngestApi, IngestApiConfig, ConsumptionApi
from pydantic import BaseModel
 
class DataModel(BaseModel):
    primary_key: Key[str]
    name: str
 
# Create a ClickHouse table
clickhouse_table = OlapTable[DataModel]("TableName")
 
# Create a Redpanda streaming topic
redpanda_topic = Stream[DataModel]("TopicName", StreamConfig(
    destination=clickhouse_table,
))
 
# Create an ingest API endpoint
ingest_api = IngestApi[DataModel]("post-api-route", IngestApiConfig(
    destination=redpanda_topic,
))
 
# Create a consumption API endpoint
class QueryParams(BaseModel):
    limit: int = 10
 
def handler(client, params: QueryParams):
    return client.query.execute("SELECT * FROM {table: Identifier} LIMIT {limit: Int32}", {
        "table": clickhouse_table.name,
        "limit": params.limit,
    })
 
consumption_api = ConsumptionApi[RequestParams, DataModel]("get-api-route", query_function=handler)
``` 
</Tabs.Tab>
</Tabs>

<br />

## Fundamentals

<StaggeredCards>
<StaggeredCard>
 <StaggeredContent
      isMooseModule={true}
      title="Data Models"
      description="Define your schema once, get validation everywhere—catch errors at dev time, not in production."
      cta={{
        label: "Learn more",
        href: PathConfig.dataModels.path,
        variant: "outline",
      }}
    />
    <StaggeredCode>
      ```ts
      interface Event {
        id: Key<string>;
        name: string;
        createdAt: Date;
      }

      interface AggregatedEvent {
        count: number;
        name: string;
      }
      ```
    </StaggeredCode>
  </StaggeredCard>

  <StaggeredCard className="border-t">
    <StaggeredContent
      isMooseModule={true}
      title="Runtime"
      description="Local dev and production execution environment for analytical backends"
      cta={{
        label: "Learn more",
        href: PathConfig.runtime.path,
        variant: "outline",
      }}
    />
    <StaggeredCode>
      ```bash
      # Start local dev server
      moose dev

      ⡏ Starting local infrastructure
        Successfully started containers
        Validated clickhousedb-1 docker container
        Validated redpanda-1 docker container
        Successfully validated red panda cluster
        Validated temporal docker container
        Successfully ran local infrastructure
      ```
    </StaggeredCode>
  </StaggeredCard>
</StaggeredCards>


## Modules
<StaggeredCards>
  
  <StaggeredCard className="border-t">
    <StaggeredContent
      isMooseModule={true}
      title="OLAP"
      description="Store and query data with ClickHouse's columnar engine—100x faster than traditional databases for analytics. Manage tables, materialized views, and data migrations in code."
      cta={{
        label: "Learn more",
        href: PathConfig.olap.path,
        variant: "outline",
      }}
    />
    <StaggeredCode>
      ```ts
      const table = new OlapTable<Event>("events");

      const mv = new MaterializedView<AggregatedEvent>({
        selectStatement: sql`
          SELECT count(*) as count, name
          FROM ${table}
          GROUP BY name
        `,
        selectTables: [table],
        tableName: "events",
        materializedViewName: "aggregated_events"
      });
      ```
    </StaggeredCode>
  </StaggeredCard>

  <StaggeredCard className="border-t">
    <StaggeredContent
      isMooseModule={true}
      title="Streaming"
      description="Build real-time data pipelines with Kafka/Redpanda streams and transformations in code"
      cta={{
        label: "Learn more",
        href: PathConfig.streaming.path,
        variant: "outline",
      }}
    />
    <StaggeredCode>
      ```ts
      const stream = new Stream<Event>("events", {
        destination: table,
      });

      stream.addConsumer((event) => {
        console.log(event);
      });
      ```
    </StaggeredCode>
  </StaggeredCard>

  <StaggeredCard className="border-t">
    <StaggeredContent
      isMooseModule={true}
      title="Workflows"
      description="Build ETL pipelines and task orchestration with in-memory workflow code"
      cta={{
        label: "Learn more",
        href: PathConfig.workflows.path,
        variant: "outline",
      }}
    />
    <StaggeredCode>
      ```ts
      const etl = new Workflow("my_etl", {
        startingTask: startEtl,
        schedule: "@every 1h",
        retries: 3,
      });
      ```
    </StaggeredCode>
  </StaggeredCard>


  <StaggeredCard className="border-t">
    <StaggeredContent
      isMooseModule={true}
      title="APIs"
      description="Type-safe ingestion and query endpoints with auto-generated OpenAPI docs."
      cta={{
        label: "Learn more",
        href: PathConfig.apis.path,
        variant: "outline",
      }}
    />
    <StaggeredCode>
      ```ts
      const postEvent = new IngestApi<Event>("post-event", {
        destination: stream,
      });
     
      const getEvents = new ConsumptionApi<Params, Event>("get-events", {
        async handler({limit = 10}, {client, sql}) {
          // query database and return results
          return await client.query.execute(sql`
            SELECT * FROM events LIMIT ${limit}
          `);
        }
      });
      ```
    </StaggeredCode>
  </StaggeredCard>

</StaggeredCards>


<Callout title="Modular Design" type="info" icon={Info}>
Each module is independent and can be used on its own. You can start with one capability and incrementally adopt more over time. 
</Callout>


## Tooling

<StaggeredCards>

  <StaggeredCard className="border-t">
    <StaggeredContent
      isMooseModule={true}
      title="Build"
      description="Easily package up your application for deploying to production"
      cta={{
        label: "Learn more",
        href: PathConfig.build.path,
        variant: "outline",
      }}
    />
    <StaggeredCode>
      ```bash
      # Build for production
      moose build
      ```
    </StaggeredCode>
  </StaggeredCard>


  <StaggeredCard className="border-t">
    <StaggeredContent
      isMooseModule={true}
      title="Plan"
      description="Preview and validate infrastructure changes before deployment with detailed schema diffs, migration planning, and rollback capabilities"
      cta={{
        label: "Learn more",
        href: PathConfig.plan.path,
        variant: "outline",
      }}
    />
    <StaggeredCode>
      ```bash
      # Create a plan
      moose plan

      # Example plan output:
        ~ Table events with column changes: [
          Added(
            Column { 
              name: "status", 
              data_type: String,
              required: true, 
              unique: false, 
              primary_key: false, 
              default: None 
            })] 
          and order by changes: OrderByChange { 
            before: [], after: [] 
          }
      
      ```
    </StaggeredCode>
  </StaggeredCard>

</StaggeredCards>


## Technology Partners

- [ClickHouse](https://clickhouse.com/) (Online Analytical Processing (OLAP) Database)
- [Redpanda](https://redpanda.com/) (Streaming)
- [Temporal](https://temporal.io/) (Workflow Orchestration)
- [Redis](https://redis.io/) (Internal State Management) 


