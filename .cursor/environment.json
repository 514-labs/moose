{
  "dockerFile": ".cursor/Dockerfile",
  "install": "pnpm install --frozen-lockfile",
  "start": "sudo systemctl start docker || echo 'Failed to start Docker. Ensure Docker is running manually.' && echo 'Environment setup complete. Ready for development!'",
  "terminals": [
    {
      "name": "Moose Development",
      "command": "echo 'Run: moose dev' && echo 'This will start ClickHouse, Redpanda, Temporal, and Redis containers' && bash"
    }
  ]
}
