---
title: 5-Minute Quickstart
description: Build your first analytical backend with Moose in 5 minutes
---

import { Tabs, Steps, Card, FileTree } from "nextra/components";
import { Callout, FeatureCard, FeatureGrid, ToggleBlock, ZoomImg, Python, TypeScript, LanguageSwitcher, MuxVideo, BulletPointsCard, Contact } from "@/components";
import { RectangleEllipsis, Square, Workflow, Database, FileCode, Play, CheckCircle, Clock } from "lucide-react";
import { paths } from "@/lib/paths";

# 5-Minute Quickstart

**Build a complete analytical backend with real-time streaming, APIs, and a database.** No configuration required.

<div className="flex items-center gap-4 my-4 text-sm text-muted-foreground">
  <div className="flex items-center gap-2">
    <Clock className="w-4 h-4" />
    <span>5 minutes to working pipeline</span>
  </div>
  <div className="flex items-center gap-2">
    <CheckCircle className="w-4 h-4" />
    <span>Zero configuration</span>
  </div>
</div>

<LanguageSwitcher />

<TypeScript>
<MuxVideo 
  playbackId="ayaB9Q00q9RIePJLfErDyRnCIOpGpHmVNSX7xy02SBGN8"
  title="Moose Quickstart Video"
  width="100%"
  height="auto"
  poster="https://image.mux.com/ayaB9Q00q9RIePJLfErDyRnCIOpGpHmVNSX7xy02SBGN8/thumbnail.png?width=214&height=121&time=28"
/>
</TypeScript>

<Python>
<MuxVideo 
  playbackId="7b6BmW9005WdBQnBrA1l2dHKNvLKyKIrlz9700k9NV71o"
  title="Moose Quickstart Video"
  width="100%"
  height="auto"
  poster="https://image.mux.com/7b6BmW9005WdBQnBrA1l2dHKNvLKyKIrlz9700k9NV71o/thumbnail.png?width=214&height=121&time=29"
/>
</Python>

## What You'll Build

By the end of this tutorial, you'll have:

<BulletPointsCard
  bulletStyle="check"
  title="A complete data pipeline with:"
  bullets={[
    { title: "HTTP API for data ingestion with automatic validation" },
    { title: "Real-time streaming with Redpanda/Kafka" },
    { title: "ClickHouse database with auto-generated schema" },
    { title: "Analytics API for querying your data" },
    { title: "Hot-reload development environment" }
  ]}
/>

## Prerequisites

<div className="grid grid-cols-1 md:grid-cols-2 gap-4 my-6">
  <div className="p-4 border border-border rounded-lg">
    <h3 className="font-semibold mb-2">Required</h3>
    <ul className="space-y-1 text-sm">
      <TypeScript>
      <li>• <strong>Node.js 20+</strong> - <a href="https://nodejs.org/en/download" className="text-blue-600 hover:underline">Download here</a></li>
      </TypeScript>
      <Python>
      <li>• <strong>Python 3.12+</strong> - <a href="https://www.python.org/downloads/" className="text-blue-600 hover:underline">Download here</a></li>
      </Python>
      <li>• <strong>Docker Desktop</strong> - <a href="https://docs.docker.com/get-started/get-docker/" className="text-blue-600 hover:underline">Download here</a></li>
    </ul>
  </div>
  
  <div className="p-4 border border-border rounded-lg">
    <h3 className="font-semibold mb-2">Supported Platforms</h3>
    <ul className="space-y-1 text-sm text-muted-foreground">
      <li>• macOS (Intel & Apple Silicon)</li>
      <li>• Linux (Ubuntu, Debian, etc.)</li>
      <li>• Windows (via WSL2)</li>
    </ul>
  </div>
</div>

<Callout type="info" title="Already have ClickHouse running?">
Skip the tutorial and add Moose as a layer on top of your existing database → [Add to ClickHouse](/moose/getting-started/from-clickhouse)
</Callout>

## Step 1: Install Moose (30 seconds)

```bash filename="One-line install" copy
bash -i <(curl -fsSL https://fiveonefour.com/install.sh) moose
```

<Callout type="success" title="✅ Success indicator">
You should see: `Moose CLI installed successfully!`
</Callout>

## Step 2: Create Your Project (1 minute)

<Steps>
### Initialize your project

<TypeScript>
```bash filename="Terminal" copy
moose init my-analytics-app typescript
cd my-analytics-app
npm install
```
</TypeScript>

<Python>
```bash filename="Terminal" copy
moose init my-analytics-app python
cd my-analytics-app

# Create virtual environment (recommended)
python3 -m venv .venv
source .venv/bin/activate

# Install dependencies
pip install -r requirements.txt
```
</Python>

### Start your development environment

```bash filename="Terminal" copy
moose dev
```

<Callout type="success" title="✅ Success indicator">
You should see output like:
```txt
✅ ClickHouse database ready
✅ Redpanda streaming ready  
✅ HTTP APIs ready
🚀 Moose dev server running on http://localhost:4000
```
</Callout>

</Steps>

## Step 3: Understand Your Project (1 minute)

Your project includes a complete example pipeline:

<Python>
<FileTree>
<FileTree.Folder name="app" defaultOpen>
<FileTree.File name="main.py" />
<FileTree.Folder name="ingest" defaultOpen>
<FileTree.File name="models.py" />
<FileTree.File name="transform.py" />
</FileTree.Folder>
<FileTree.Folder name="views">
<FileTree.File name="bar_aggregated.py" />
</FileTree.Folder>
<FileTree.Folder name="apis">
<FileTree.File name="bar.py" />
</FileTree.Folder>
</FileTree.Folder>
</FileTree>
</Python>

<TypeScript>
<FileTree>
<FileTree.Folder name="app" defaultOpen>
<FileTree.File name="index.ts" />
<FileTree.Folder name="ingest" defaultOpen>
<FileTree.File name="models.ts" />
<FileTree.File name="transform.ts" />
</FileTree.Folder>
<FileTree.Folder name="views">
<FileTree.File name="barAggregated.ts" />
</FileTree.Folder>
<FileTree.Folder name="apis">
<FileTree.File name="bar.ts" />
</FileTree.Folder>
</FileTree.Folder>
</FileTree>
</TypeScript>

<BulletPointsCard
  title="Data Flow:"
  bulletStyle="number"
  bullets={[
    { title: "Raw data enters via `/ingest/Foo` HTTP endpoint" },
    { title: "Data is transformed in real-time (Foo → Bar)" },
    { title: "Transformed data lands in ClickHouse" },
    { title: "Materialized view creates aggregations" },
    { title: "Analytics API exposes queryable data" }
  ]}
/>

## Step 4: Test Your Pipeline (2 minutes)

### Send test data

Generate sample data to see your pipeline in action:

<TypeScript>
```bash filename="Terminal" copy
moose workflow run generator
```
</TypeScript>

<Python>
```bash filename="Terminal" copy
moose workflow run generator
```
</Python>

<Callout type="success" title="✅ Success indicator">
You should see logs like:
```txt
POST ingest/Foo
[POST] Data received at ingest API sink for Foo
Received Foo_0_0 -> Bar_0_0 1 message(s)
[DB] 17 row(s) successfully written to DB table (Bar)
```
</Callout>

### Query your data

Test your analytics API:

```bash filename="Terminal" copy
curl "http://localhost:4000/consumption/bar"
```

<Callout type="success" title="✅ Success indicator">
You should see JSON data with your processed events:
```json
[
  {
    "id": "123",
    "timestamp": "2024-01-15T10:30:00Z",
    "processed_data": "..."
  }
]
```
</Callout>

### Explore with OpenAPI UI

1. Install the [OpenAPI (Swagger) Viewer extension](https://marketplace.cursorapi.com/items?itemName=42Crunch.vscode-openapi) in your IDE
2. Open `.moose/openapi.yaml` in your IDE  
3. Click the "Preview" icon to launch the interactive API explorer
4. Test the `POST /ingest/Foo` and `GET /consumption/bar` endpoints

<MuxVideo 
  playbackId="66qLKy46JddIBcJWqestllPmBJBxWcOwbgw00WpKXO02M"
  title="Using the OpenAPI Swagger UI for Moose APIs"
  width="100%"
  height="auto"
  autoPlay={true}
  muted={true}
  loop={true}
/>

## Step 5: Hot Reload Schema Changes (1 minute)

1. Open <TypeScript inline>`app/ingest/models.ts`</TypeScript><Python inline>`app/ingest/models.py`</Python>
2. Add a new field to your data model:

<TypeScript>
```ts filename="app/ingest/models.ts" {5} copy
export interface Foo {
  id: Key<string>;
  timestamp: Date;
  data: string;
  newField: string; // Add this line
}
```
</TypeScript>

<Python>
```python filename="app/ingest/models.py" {6} copy
class Foo(BaseModel):
    id: Key[str]
    timestamp: datetime
    data: str
    new_field: str  # Add this line
```
</Python>

3. Save the file and watch your terminal

<Callout type="success" title="✅ Success indicator">
You should see Moose automatically update your infrastructure:
```txt
~ Table Foo with column changes: [Added(Column { name: "newField", data_type: String })]
~ API Endpoint: Foo - Updated validation schema
~ Topic: Foo - Schema updated
```
</Callout>

**That's it!** Your API, database schema, and streaming topic all updated automatically.

## 🎉 Congratulations!

You've built a complete analytical backend with:

<BulletPointsCard
  bulletStyle="check"
  title="✅ What's Working"
  bullets={[
    { title: "Type-safe data ingestion API" },
    { title: "Real-time stream processing" },
    { title: "ClickHouse database with auto-schema" },
    { title: "Analytics API with filtering" },
    { title: "Hot-reload development" }
  ]}
/>
  

## Next Steps

<FeatureGrid columns={3}>
<FeatureCard
  title="Data Modeling"
  description="Learn schema design patterns"
  href={paths.dataModels}
  Icon={Database}
/>
<FeatureCard
  title="Stream Processing"
  description="Transform data in real-time"
  href={paths.streams}
  Icon={Square}
/>
<FeatureCard 
  title="Analytics APIs"
  description="Build powerful query endpoints"
  href={paths.consumptionApis}
  Icon={FileCode}
/>
</FeatureGrid>

## Need Help?

<ToggleBlock openText="Common Issues & Solutions" closeText="Hide troubleshooting">
<Tabs items={["Docker Issues", "Version Issues", "Port Conflicts", "Permission Issues"]}>
<Tabs.Tab>
**Docker not running:**
```bash filename="Terminal" copy
# macOS
open -a Docker

# Linux
sudo systemctl start docker

# Verify Docker is running
docker ps
```

**Docker out of space:**
```bash filename="Terminal" copy
docker system prune -a
```
</Tabs.Tab>

<Tabs.Tab>
<TypeScript>
**Node.js version too old:**
```bash filename="Terminal" copy
# Check version
node -v

# Install Node 20+ with nvm
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
nvm install 20
nvm use 20
```
</TypeScript>

<Python>
**Python version too old:**
```bash filename="Terminal" copy
# Check version  
python3 --version

# Install Python 3.12+ with pyenv
curl https://pyenv.run | bash
pyenv install 3.12
pyenv local 3.12
```
</Python>
</Tabs.Tab>

<Tabs.Tab>
**Port 4000 already in use:**
```bash filename="Terminal" copy
# Find what's using port 4000
lsof -i :4000

# Kill the process (replace PID)
kill -9 <PID>

# Or use a different port
moose dev --port 4001
```
</Tabs.Tab>

<Tabs.Tab>
**Permission denied:**
```bash filename="Terminal" copy
# Fix Docker permissions (Linux)
sudo usermod -aG docker $USER
newgrp docker

# Fix file permissions
chmod +x ~/.moose/bin/moose
```
</Tabs.Tab>
</Tabs>

**Still stuck?** Join our [Discord community](https://discord.gg/moose) or [open an issue](https://github.com/514-labs/moose/issues).
</ToggleBlock>


<Contact />