language = "typescript" # Must be typescript or python
description = "A template ingesting goodreads data from Kaggle"
post_install_print = """
📂 Go to your project directory:
	$ cd {project_dir}

📦 Install Dependencies:
    $ npm install

🛠️ Start dev server:
	$ moose dev

🔑 Add your Kaggle credentials, kaggle.json to ~/.kaggle/kaggle.json per the Kaggle SDK instructions. 
    If you don't have those credentials, see https://www.kaggle.com/docs/api.

🏃‍♂️ Run python ingest script
    $ pip install -r get_data/requirements.txt
    $ python get_data/ingest_goodreads_data.py
"""

# we might need to add python dependencies install file