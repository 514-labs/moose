import { Callout } from 'nextra/components'
import { Tabs } from 'nextra/components'

# Capture data
MooseJS makes it easy to capture data from your user facing applications by providing typed SDKs which are automatically generated from your data models. 

## From MooseJS SDKs
<Callout type="info" emoji="ℹ️">
  We currently support Typescript SDKs. Support for other languages is coming soon.
</Callout>

You can develop your data service right along side your user facing applications. MooseJS will automatically generate a Typescript SDK as you build your service locally and make them available for you to link to your application. To link a data service's SDK to your application, simply run the following command:

```txt filename="Terminal"
// From your application's root directory or wherever you run npm install
npm link <project-name>-sdk 
```

## Through Ingestion Points
As you define data models in your service, MooseJS will automatically generate ingestion points for you to send data to. Ingestion points are HTTP endpoints that you can send data to.

<Tabs items={['curl', 'javascript', 'python']}>
  <Tabs.Tab>
  ``` bash
  curl -X POST \
    -H "Content-Type: application/json" \
    -d '{"name": "Test Name", "description": "This is a test description"}' \
    http://localhost:4000/ingest/user
  ```
  </Tabs.Tab>
  <Tabs.Tab>
  ```javascript
  fetch('http://localhost:4000/ingest/user', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      name: 'Test Name',
      description: 'This is a test description'
    })
  })
  ```
  </Tabs.Tab>
  <Tabs.Tab>
  ```python
    import requests

    requests.post('http://localhost:4000/ingest/user', json={
      'name': 'Test Name',
      'description': 'This is a test description'
    })
    ```
  </Tabs.Tab>
</Tabs>