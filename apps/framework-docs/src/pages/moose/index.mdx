---
title: Moose - Backend Framework for the Analytics Stack
description: Open source TypeScript/Python framework for building analytical backends with real-time streaming, API generation, and type-safe data models
image: /moose-og-image.png
---

import {
  CTACard,
  CTACards,
  ZoomImg,
  ChipButton,
  Columns,
  Column,
  FeatureCard,
  FeatureGrid,
  BulletPointsCard,
  Callout,
  QnABullets,
  CheckmarkBullets,
  Icons,
  Contact,
  InlineProps,
  StaggeredCard,
  StaggeredContent,
  StaggeredCode,
} from "@/components";
import { Button } from "@/components/ui";
import { SmallTextEmbed } from "@/components/typography";
import { 
  FolderPlus, 
  Scroll, 
  Code, 
  Terminal,
  Database, 
  Zap, 
  FileCode, 
  Clock, 
  Users, 
  GitBranch, 
  HardDriveDownload, 
  Waves,
  Star,
  Download,
  TrendingUp,
  Sparkles,
  ShieldCheck,
  MapPin
} from "lucide-react";
import Link from "next/link";
import { Tabs, Tab } from "nextra/components";
import { paths } from "@/lib/paths";

# Welcome to Moose

<InlineProps
  items={[
    {
      Icon: Clock,
      text: "5 minute setup",
    },
    {
      Icon: ShieldCheck,
      text: "Pre-integrated ClickHouse, Redpanda, Temporal",
    },
    {
      Icon: TrendingUp,
      text: "Hot-reload development",
    }
  ]}
/>

```bash filename="Get started in 30 seconds" copy
bash -i <(curl -fsSL https://fiveonefour.com/install.sh) moose
```

<CTACards columns={3}>
  <CTACard
    title="Try the 5-minute tutorial"
    description="Build your first analytics backend from scratch"
    ctaLink={paths.quickstart}
    ctaLabel="Start Tutorial →"
    Icon={TrendingUp}
    variant="default"
  />
  <CTACard
    title="Add to existing ClickHouse"
    description="Works with your current data - zero migration required"
    ctaLink="/moose/getting-started/from-clickhouse"
    ctaLabel="Add Moose Layer"
    Icon={Database}
    variant="default"
  />
  <CTACard
    title="Star on GitHub"
    description="250+ developers building with Moose"
    ctaLink="https://github.com/514-labs/moose"
    ctaLabel="⭐ Star on GitHub"
    Icon={Star}
    variant="default"
  />
</CTACards>

## What is Moose?
Open source TypeScript/Python framework for building analytical backends—streaming, storage, and APIs in TypeScript or Python.

<BulletPointsCard
  title="Why Devs Choose Moose"
  bulletStyle="check"
  compact={true}
  bullets={[
    { title: "Define everything in code", description: "Data models, pipelines, and endpoints." },
    { title: "Integrated analytics infrastructure", description: "Get ClickHouse tables, Kafka/Redpanda streams, and HTTP APIs, all auto-wired and type-safe." },
    { title: "Fast setup and local-first development", description: "Zero boilerplate, instant feedback, and no context switching." },
  ]}
/>

<Tabs items={["TypeScript", "Python"]}>
<Tabs.Tab>
```ts filename="Complete Analytical Backend in 1 TS file" copy
import { Key, OlapTable, Stream, IngestApi, ConsumptionApi } from "@514labs/moose-lib";
 
interface DataModel {
  primaryKey: Key<string>;
  name: string;
}
// Create a ClickHouse table
export const clickhouseTable = new OlapTable<DataModel>("TableName");
 
// Create a Redpanda streaming topic
export const redpandaTopic = new Stream<DataModel>("TopicName", {
  destination: clickhouseTable,
});
 
// Create an ingest API endpoint
export const ingestApi = new IngestApi<DataModel>("post-api-route", {
  destination: redpandaTopic,
});
 
// Create consumption API endpoint
interface QueryParams {
  limit?: number;
}
export const consumptionApi = new ConsumptionApi<QueryParams, DataModel>("get-api-route", {
  async handler({limit = 10}: QueryParams, {client, sql}): {
    const result = await client.query.execute(sql`SELECT * FROM ${clickhouseTable} LIMIT ${limit}`);
    return await result.json();
  }
});
```
</Tabs.Tab>
<Tabs.Tab>
```python filename="Complete Analytical Backend in 1 Python file" copy
from moose_lib import Key, OlapTable, Stream, StreamConfig, IngestApi, IngestApiConfig, ConsumptionApi
from pydantic import BaseModel
 
class DataModel(BaseModel):
    primary_key: Key[str]
    name: str
 
# Create a ClickHouse table
clickhouse_table = OlapTable[DataModel]("TableName")
 
# Create a Redpanda streaming topic
redpanda_topic = Stream[DataModel]("TopicName", StreamConfig(
    destination=clickhouse_table,
))
 
# Create an ingest API endpoint
ingest_api = IngestApi[DataModel]("post-api-route", IngestApiConfig(
    destination=redpanda_topic,
))
 
# Create a consumption API endpoint
class QueryParams(BaseModel):
    limit: int = 10
 
def handler(client, params: QueryParams):
    return client.query.execute("SELECT * FROM {table: Identifier} LIMIT {limit: Int32}", {
        "table": clickhouse_table.name,
        "limit": params.limit,
    })
 
consumption_api = ConsumptionApi[RequestParams, DataModel]("get-api-route", query_function=handler)
``` 
</Tabs.Tab>
</Tabs>


## Why Moose Exists & Who It's For

Moose is for developers who want to move fast and treat analytics as real software—not a patchwork of YAML, SQL, and fragile connectors. 

<div className="grid grid-cols-1 md:grid-cols-2 gap-6">
  <BulletPointsCard
    title="Web DX: Fast & Integrated"
    bulletStyle="check"
    compact={true}
    bullets={[
      { title: "Frameworks out of the box", description: "Next, Django, Rails" },
      { title: "Integrated database", description: "Postgres/MySQL built-in" },
      { title: "Built-in HTTP APIs", description: "Routing & endpoints included" },
      { title: "Type-safe data models", description: "ORMs and dev time validation" },
      { title: "Hot-reload local development", description: "Instant feedback on changes" },
      { title: "One codebase, one deployment", description: "Get to production in minutes" }
    ]}
  />
  <BulletPointsCard
    title="Analytics DX: Slow & Fragmented"
    bulletStyle="x"
    compact={true}
    bullets={[
      { title: "No frameworks", description: "Manual tool integration" },
      { title: "Separate database setup", description: "Manual ClickHouse configuration" },
      { title: "Custom API boilerplate", description: "FastAPI/Express from scratch" },
      { title: "Manual schema management", description: "Write DDL scripts by hand, YAML configs everywhere" },
      { title: "No hot-reload dev", description: "Wait for deploys to see errors" },
      { title: "Multiple codebases and deployments", description: "5+ separate systems to manage" }
    ]}
  />
</div>

### Without Moose vs. With Moose
Moose gives you a unified, code-first experience to build, test, and ship analytics features with the same speed and confidence as web apps.

<div className="grid grid-cols-1 md:grid-cols-2 gap-6">
  <div>
    ```txt filename="Without Moose: Manual config & integration" 
    Kafka Config     (300 lines of YAML)
    + ClickHouse     (Manual schema management)  
    + dbt            (SQL-only transformations)
    + Airflow        (Workflow orchestration)
    + Custom APIs    (Boilerplate code)
    + ...
    ```
  </div>
  <div>
    ```ts filename="With Moose: One codebase for everything" copy
    new IngestPipeline<DataModel>("pipeline", {
      ingest: true, // HTTP API w/ validation
      stream: true, // Redpanda/Kafka topic
      table: true, // ClickHouse table
    });
    // Auto-generated & auto-wired together
    ```
  </div>
</div>

<div className="grid grid-cols-1 md:grid-cols-2 gap-6 mt-4">
  <div>
    <BulletPointsCard
      title="Constant context switching"
      bulletStyle="x"
      compact={true}
      bullets={[
        "Does my Database table use snake_case or camelCase?",
        "Did I add the new field to both the model AND the table?",
        "Which database field was nullable again?",
      ]}
    />
  </div>
  <div>
    <BulletPointsCard
      title="Similar Web DX, but for analytics"
      bulletStyle="check"
      compact={true}
      bullets={[
        "Zero glue code",
        "Type-safe, code-first",
        "Integrated HTTP, Redpanda, ClickHouse",
        "Hot-reload development",
      ]}
    />
  </div>
</div>


## Core Capabilities

<div className="space-y-8 my-8">
  {/* Type-Safe Data Models */}
  <StaggeredCard>
    <StaggeredContent>
      ### Type-Safe Data Models
      Define your schema once, get validation everywhere—catch errors at dev time, not in production.
      <Button asChild variant="link">
        <a href={paths.dataModels}>Learn about data models →</a>
      </Button>
    </StaggeredContent>
    <StaggeredCode>
      ```ts
      interface Event {
        id: Key<string>;
        name: string;
        createdAt: Date;
      }
      ```
    </StaggeredCode>
  </StaggeredCard>

  {/* HTTP APIs */}
  <StaggeredCard>
    <StaggeredContent>
      ### HTTP APIs
      Expose data ingestion and analytics endpoints automatically—no boilerplate code needed.
      <Button asChild variant="link">
        <a href={paths.ingestion}>Explore ingest APIs →</a>
      </Button>
    </StaggeredContent>
    <StaggeredCode>
      ```ts
      const postApi = new IngestApi<Event>("post-event", {
        destination: stream,
      });
      ```
    </StaggeredCode>
  </StaggeredCard>

  {/* Real-Time Streaming */}
  <StaggeredCard>
    <StaggeredContent>
      ### Real-Time Streaming
      Process events as they arrive with built-in Kafka/Redpanda—no separate streaming infrastructure to manage.
      <Button asChild variant="link">
        <a href={paths.streams}>Explore streaming →</a>
      </Button>
    </StaggeredContent>
    <StaggeredCode>
      ```ts
      const stream = new Stream<Event>("events", {
        destination: table,
      });
      ```
    </StaggeredCode>
  </StaggeredCard>

  {/* Analytical Database */}
  <StaggeredCard>
    <StaggeredContent>
      ### Analytical Database
      Store and query data with ClickHouse's columnar engine—100x faster than traditional databases for analytics.
      <Button asChild variant="link">
        <a href={paths.olapTables}>Learn about OLAP tables →</a>
      </Button>
    </StaggeredContent>
    <StaggeredCode>
      ```ts
      const table = new OlapTable<Event>("events");

      const sql = sql`SELECT count(*) FROM ${table} GROUP BY name`;

      const matView = new MaterializedView<Event>("aggregated-events", {
        selectStatement: sql,
        selectTables: [table],
        //...
      ```
    </StaggeredCode>
  </StaggeredCard>

  {/* Consumption APIs */}
  <StaggeredCard>
    <StaggeredContent>
      ### Consumption APIs
      Expose analytical data via HTTP endpoints, with type safety and schema validation.
      <Button asChild variant="link">
        <a href={paths.consumptionApis}>Explore consumption APIs →</a>
      </Button>
    </StaggeredContent>
    <StaggeredCode>
      ```ts
      const getEvents = new ConsumptionApi<Params, Event>("get-events", {
        async handler({ limit = 10 }, { client, sql }) {
          const result = await client.query.execute(
            sql`SELECT * FROM ${table} LIMIT ${limit}`
          );
          return await result.json();
        }
      });
      ```
    </StaggeredCode>
  </StaggeredCard>

  {/* Data Movement Workflows */}
  <StaggeredCard>
    <StaggeredContent>
      ### Data Movement Workflows
      Orchestrate background jobs and scheduled tasks to move data between your analytical database and other systems.
      <Button asChild variant="link">
        <a href={paths.workflows}>Learn about workflows →</a>
      </Button>
    </StaggeredContent>
    <StaggeredCode>
      ```ts
      const extractData = new Task<null, boolean>("extract-data", {
        run: async () => {
          const data = await fetch(
            "https://api.example.com/data"
          ).then(res => res.json());
          await table.insert(data);
          return true;
        },
      });

      const workflow = new Workflow("hourly-etl", {
        schedule: "0 * * * *",
        firstTask: extractData,
      });
      ```
    </StaggeredCode>
  </StaggeredCard>
</div>

## Start Developing in Seconds

<BulletPointsCard
  title="Local-First Development"
  bulletStyle="check"
  compact={true}
  bullets={[
    "1 command to spin up entire backend infrastructure",
    "Zero configuration required",
    "Exact replication of production infrastructure and logic",
    "See your changes reflected instantly throughout the stack",
  ]}
/>

Moose comes with a pre-configured and fully integrated data stack that runs entirely on your laptop. Spin it up with one command:

```bash filename="Terminal" copy
moose dev
```

All the infrastructure is automatically spun up:
```txt
⡏ Starting local infrastructure
  Successfully started containers
     Validated clickhousedb-1 docker container
     Validated redpanda-1 docker container
  Successfully validated red panda cluster
     Validated temporal docker container
  Successfully ran local infrastructure
```

<BulletPointsCard
  title="Common workflow scenarios"
  bulletStyle="check"
  compact={true}
  bullets={[
    { title: "Need to add or change a model?", description: "Moose hot reloads it to your local infrastructure when you save" },
    { title: "Added a new field?", description: "It's instantly available in your API, streams, and database" },
    { title: "Need to test your pipeline?", description: "Send sample data to your local ingest API and see the data flow through" },
  ]}
/>

Learn more in the [5-minute tutorial](/moose/getting-started/quickstart).

### Common Use Cases

<CTACards columns={3}>
<CTACard
  title="User-Facing Analytics"
  description="Embed real-time dashboards and metrics in your product. Moose handles event ingestion, streaming, aggregation, and exposes APIs for your frontend."
  ctaLink={`${paths.architecture}#user-facing-analytics`}
  ctaLabel="Learn More →"
  Icon={MapPin}
/>
<CTACard
  title="Operational Data Warehouse"
  description="Centralize business data from SaaS, databases, and apps. Moose orchestrates ingestion, transformation, and storage in ClickHouse for BI tools."
  ctaLink={`${paths.architecture}#operational-data-warehouse`}
  ctaLabel="Learn More →"
  Icon={MapPin}
/>
<CTACard
  title="Observability"
  description="Monitor application and infrastructure metrics/logs at scale. Moose ingests telemetry, processes streams, and exposes data for monitoring platforms."
  ctaLink={`${paths.architecture}#observability`}
  ctaLabel="Learn More →"
  Icon={MapPin}
/>
</CTACards>

<Callout type="info" title="Ready to build?">
  Head to the [5-minute tutorial](/moose/getting-started/quickstart) to try Moose yourself!
</Callout>