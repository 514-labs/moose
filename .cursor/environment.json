{
  "dockerFile": ".cursor/Dockerfile",
  "install": "pnpm install --frozen-lockfile",
  "start": "sudo service docker start && echo 'Environment setup complete. Ready for development!'",
  "terminals": [
    {
      "name": "Moose Development",
      "command": "echo 'Run: moose dev' && echo 'This will start ClickHouse, Redpanda, Temporal, and Redis containers' && echo 'GitHub MCP available for PR comment monitoring' && bash"
    }
  ]
}
