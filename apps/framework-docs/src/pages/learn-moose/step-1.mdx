import { Callout } from "../../components";
import { Steps, FileTree } from "nextra/components";

# Create a New Project and Data Model

<Callout type="info" title="Make Sure Docker is Running">
Open Docker Desktop if its not already running (run `docker info` in your terminal to verify).

Don't have it installed? [Download Docker here](https://www.docker.com/products/docker-desktop)

</Callout>

## Create an Empty Moose Project

<Steps>
### Install the Moose CLI
```bash filename="Terminal" copy
npx create-moose-app@latest moose-github-analytics typescript
```
This Moose CLI helper function creates a new Moose app with the proper conventions for the basic directory structure. This includes an /app folder which contains the heart of your new Moose app. Each sub-folder containing one type of Moose primitive.

<FileTree>
  <FileTree.Folder open title="moose-github-analytics">
    <FileTree.Folder open title="app">
      <FileTree.Folder title="datamodels" />
      <FileTree.Folder title="functions" />
      <FileTree.Folder title="blocks" />
      <FileTree.Folder title="apis" />
    </FileTree.Folder>
  </FileTree.Folder>
</FileTree>

</Steps>
