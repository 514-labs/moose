## Managing Changes With Existing Tables

We're working on a new developer experience for how to manage schema changes in your database.

### What is a schema change?

A schema change is a change to the database schema. This can be a new table, a new column, a new index, a new constraint, etc.

### How do I manage schema changes?

We're working on a new developer experience for how to manage schema changes in your database.