[build-system]
requires = ["maturin==1.7.0"] 
build-backend = "maturin" 

[project]
name = "moose-cli" 
description = "Moose CLI" 
requires-python = ">=3.7" 

[tool.maturin]
bindings = "bin" 
strip = true 
